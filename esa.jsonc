// 阿里云 ESA Pages 构建配置（jsonc 支持注释）
// 官方文档：https://help.aliyun.com/zh/edge-security-acceleration/esa/user-guide/build-pages
//
// 本站为纯静态站点：
//   installCommand ""  → 跳过依赖安装（官方：空字符串即跳过）
//   buildCommand       → node build.js 把站点文件复制到 dist/
//   assets.directory   → 静态托管 dist/ 目录（注意：assets 是对象，不是字符串）
//   未配置 entry（边缘函数入口）与 notFoundStrategy（未知路径默认返回 404，
//   与 Docker 版 nginx 的 try_files =404 行为一致）。
{
  "name": "mywebsite",
  "installCommand": "",
  "buildCommand": "npm run build",
  "assets": {
    "directory": "./dist"
  }
}
